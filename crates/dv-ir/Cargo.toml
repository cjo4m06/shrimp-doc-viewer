[package]
name = "dv-ir"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Backend-agnostic display list IR shared by every doc-viewer format frontend."

[dependencies]
