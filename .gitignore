# Rust
/target
**/*.rs.bk

# Node
node_modules/
dist/
pkg/

# Generated WASM artifacts (rebuild with ./scripts/build-wasm.sh)
packages/doc-viewer/wasm/

# Local demo/test assets (fonts are lazy-loaded at runtime; large, never committed)
examples/assets/fonts/
examples/assets/realtest*
*.png
!docs/**/*.png
# but DO commit the tiny synthetic test fixtures the crate tests read
!crates/**/tests/**/*.png

# Local Pages build output (assembled by hand to test demo/ before deploy)
.pages-build/

# Local promo drafts (not part of the project)
PROMO-DRAFTS.local.md

# Editor / OS
.DS_Store
.idea/
.vscode/
